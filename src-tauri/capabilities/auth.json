{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "auth",
  "description": "Minimal IPC for the in-app OAuth login pages (Google/Microsoft/Apple) — only the core defaults the injected link handler needs to open external links in the browser. Kept separate from the default capability so these third-party pages don't inherit the notification/window-state permissions granted to the Messenger surface.",
  "windows": ["main", "win-*"],
  "remote": {
    "urls": [
      "https://accounts.google.com",
      "https://login.microsoftonline.com",
      "https://appleid.apple.com"
    ]
  },
  "permissions": ["core:default"]
}
