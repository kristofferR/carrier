{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main Carrier window. The page never calls into the Rust core, so only the defaults are granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
